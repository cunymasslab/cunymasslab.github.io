<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://cunymasslab.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://cunymasslab.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2025-12-09T04:11:39+00:00</updated><id>https://cunymasslab.github.io/feed.xml</id><title type="html">blank</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">Remote Debugging on Cluster with VS Code and DebugPy</title><link href="https://cunymasslab.github.io/blog/2025/remote-debugging-vscode/" rel="alternate" type="text/html" title="Remote Debugging on Cluster with VS Code and DebugPy"/><published>2025-10-07T12:00:00+00:00</published><updated>2025-10-07T12:00:00+00:00</updated><id>https://cunymasslab.github.io/blog/2025/remote-debugging-vscode</id><content type="html" xml:base="https://cunymasslab.github.io/blog/2025/remote-debugging-vscode/"><![CDATA[<p>Welcome to this tutorial on remote debugging! When working with machine learning models or computational tasks on a cluster, debugging can be challenging. This guide will show you how to use <strong>VS Code</strong> and <strong>DebugPy</strong> to debug Python code running on a remote cluster as if it were running locally on your machine.</p> <h2 id="why-remote-debugging">Why Remote Debugging?</h2> <p>When developing on computing clusters, you often encounter scenarios where:</p> <ul> <li>Your code runs fine locally but fails on the cluster</li> <li>You need to debug with the cluster’s specific hardware (GPUs, large memory)</li> <li>Long-running processes make print-statement debugging impractical</li> <li>You want to inspect variables and step through code in real-time</li> </ul> <p>Remote debugging solves these problems by letting you use VS Code’s powerful debugging tools while your code executes on the remote server.</p> <p><strong>Beyond the Cluster:</strong> The skills you’ll learn here extend far beyond academic computing clusters. These same techniques apply to debugging applications in cloud environments, including containerized applications running in Kubernetes pods, AWS EC2 instances, Google Cloud VMs, or Azure containers. As modern software increasingly runs in distributed and cloud-native architectures, mastering remote debugging becomes an essential skill for any developer working with production systems.</p> <hr/> <h2 id="prerequisites">Prerequisites</h2> <p>Before we begin, ensure you have:</p> <ul> <li><strong>Conda</strong> installed on your local machine (see our <a href="/blog/2025/python-environment-setup/">Python Environment Setup guide</a>)</li> <li><strong>VS Code</strong> installed on your local machine</li> <li><strong>Python Extension</strong> for VS Code installed</li> <li><strong>Automated SSH access</strong> to your remote server (covered in our <a href="/blog/2025/python-environment-setup/">environment setup guide</a>)</li> <li><strong>Git repository</strong> cloned on your local machine</li> <li>Basic familiarity with VS Code and Python</li> </ul> <p><strong>Important:</strong> Before starting setup on the remote server, open a <code class="language-plaintext highlighter-rouge">screen</code> session to avoid connection drops during installation. Learn about persistent sessions with <code class="language-plaintext highlighter-rouge">screen</code> in our <a href="/blog/2025/python-environment-setup/#practical-example-setting-up-for-lab-projects">environment setup guide</a>.</p> <hr/> <h2 id="part-1-setting-up-the-remote-server">Part 1: Setting Up the Remote Server</h2> <h3 id="step-1-start-a-persistent-session-recommended">Step 1: Start a Persistent Session (Recommended)</h3> <p>Long installations can break if your SSH connection closes. Start a persistent <code class="language-plaintext highlighter-rouge">screen</code> session to keep your work alive:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># SSH to your cluster</span>
ssh cluster_node

<span class="c"># Activate your conda environment</span>
conda activate vulnewdata

<span class="c"># Start a screen session</span>
screen <span class="nt">-S</span> setup_env

<span class="c"># Proceed with setup steps below</span>
</code></pre></div></div> <p><strong>Screen Commands:</strong></p> <ul> <li>Detach: <code class="language-plaintext highlighter-rouge">Ctrl+A</code>, then <code class="language-plaintext highlighter-rouge">D</code></li> <li>Reattach: <code class="language-plaintext highlighter-rouge">screen -r setup_env</code></li> <li>List sessions: <code class="language-plaintext highlighter-rouge">screen -ls</code></li> </ul> <p>This ensures installations or debugging sessions stay alive even if you disconnect.</p> <h3 id="step-2-install-debugpy-on-the-cluster">Step 2: Install DebugPy on the Cluster</h3> <p><a href="https://github.com/microsoft/debugpy">DebugPy</a> is Microsoft’s debug adapter for Python. It enables VS Code to attach to and debug Python processes remotely.</p> <p>In your activated conda environment, run:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>conda <span class="nb">install </span>anaconda::debugpy
</code></pre></div></div> <p>Verify the installation:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>python <span class="nt">-m</span> debugpy <span class="nt">--version</span>
</code></pre></div></div> <p>You should see the version number (e.g., <code class="language-plaintext highlighter-rouge">1.8.0</code>).</p> <h3 id="step-3-run-debugpy-on-the-remote-server">Step 3: Run DebugPy on the Remote Server</h3> <p>Before debugging your project code, let’s practice with a simple example. Create a test script or use the sample code in your <code class="language-plaintext highlighter-rouge">uncertainty/debug</code> directory.</p> <p><strong>Understanding the DebugPy Command:</strong></p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>python <span class="nt">-m</span> debugpy <span class="nt">--wait-for-client</span> <span class="nt">--listen</span> 0.0.0.0:5678 your_script.py
</code></pre></div></div> <p>Let’s break down the parameters:</p> <ul> <li><code class="language-plaintext highlighter-rouge">0.0.0.0</code> - Listens on all network interfaces (allows external connections)</li> <li><code class="language-plaintext highlighter-rouge">5678</code> - The port for the debug server (use another if this is occupied)</li> <li><code class="language-plaintext highlighter-rouge">--wait-for-client</code> - Pauses code execution until VS Code attaches</li> <li><code class="language-plaintext highlighter-rouge">your_script.py</code> - The Python file you want to debug</li> </ul> <p><strong>Create a Helper Script (Optional):</strong></p> <p>For convenience, create a <code class="language-plaintext highlighter-rouge">runpy.sh</code> script:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">#!/bin/bash</span>
<span class="c"># runpy.sh - Helper script to run Python files with DebugPy</span>
python <span class="nt">-m</span> debugpy <span class="nt">--wait-for-client</span> <span class="nt">--listen</span> 0.0.0.0:5678 <span class="s2">"</span><span class="nv">$1</span><span class="s2">"</span>
</code></pre></div></div> <p>Make it executable:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">chmod</span> +x runpy.sh
</code></pre></div></div> <p>Run your Python file with debugging:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>./runpy.sh train_model.py
</code></pre></div></div> <p>Or directly:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>python <span class="nt">-m</span> debugpy <span class="nt">--wait-for-client</span> <span class="nt">--listen</span> 0.0.0.0:5678 train_model.py
</code></pre></div></div> <h3 id="step-4-verify-debugpy-is-listening">Step 4: Verify DebugPy is Listening</h3> <p>Check that the debug server is running and waiting for connections:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>netstat <span class="nt">-a</span> <span class="nt">-n</span> | <span class="nb">grep </span>5678
</code></pre></div></div> <p>You should see output indicating port <code class="language-plaintext highlighter-rouge">5678</code> is in <code class="language-plaintext highlighter-rouge">LISTEN</code> state:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>tcp        0      0 0.0.0.0:5678            0.0.0.0:*               LISTEN
</code></pre></div></div> <p>If you see this, DebugPy is successfully waiting for your VS Code debugger to attach!</p> <hr/> <h2 id="part-2-setting-up-your-local-machine">Part 2: Setting Up Your Local Machine</h2> <h3 id="step-1-configure-vs-code-for-remote-debugging">Step 1: Configure VS Code for Remote Debugging</h3> <p>VS Code uses <code class="language-plaintext highlighter-rouge">launch.json</code> to configure debugging sessions. Let’s create one for remote debugging.</p> <p><strong>Create or Edit <code class="language-plaintext highlighter-rouge">.vscode/launch.json</code>:</strong></p> <p>In your project’s root directory, create <code class="language-plaintext highlighter-rouge">.vscode/launch.json</code> with the following configuration:</p> <div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"version"</span><span class="p">:</span><span class="w"> </span><span class="s2">"0.2.0"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"configurations"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
    </span><span class="p">{</span><span class="w">
      </span><span class="nl">"name"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Python Debugger: Remote Attach without Mappings"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"type"</span><span class="p">:</span><span class="w"> </span><span class="s2">"debugpy"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"request"</span><span class="p">:</span><span class="w"> </span><span class="s2">"attach"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"connect"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
        </span><span class="nl">"host"</span><span class="p">:</span><span class="w"> </span><span class="s2">"localhost"</span><span class="p">,</span><span class="w">
        </span><span class="nl">"port"</span><span class="p">:</span><span class="w"> </span><span class="mi">55678</span><span class="w">
      </span><span class="p">}</span><span class="w">
    </span><span class="p">},</span><span class="w">
    </span><span class="p">{</span><span class="w">
      </span><span class="nl">"name"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Python Debugger: Remote Attach"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"type"</span><span class="p">:</span><span class="w"> </span><span class="s2">"debugpy"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"request"</span><span class="p">:</span><span class="w"> </span><span class="s2">"attach"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"connect"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
        </span><span class="nl">"host"</span><span class="p">:</span><span class="w"> </span><span class="s2">"localhost"</span><span class="p">,</span><span class="w">
        </span><span class="nl">"port"</span><span class="p">:</span><span class="w"> </span><span class="mi">55678</span><span class="w">
      </span><span class="p">},</span><span class="w">
      </span><span class="nl">"pathMappings"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
        </span><span class="p">{</span><span class="w">
          </span><span class="nl">"localRoot"</span><span class="p">:</span><span class="w"> </span><span class="s2">"${workspaceFolder}"</span><span class="p">,</span><span class="w">
          </span><span class="nl">"remoteRoot"</span><span class="p">:</span><span class="w"> </span><span class="s2">"/home/vulnewdata"</span><span class="w">
        </span><span class="p">}</span><span class="w">
      </span><span class="p">]</span><span class="w">
    </span><span class="p">}</span><span class="w">
  </span><span class="p">]</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div> <p><strong>Understanding the Configuration:</strong></p> <p>We have two debug configurations:</p> <ol> <li><strong>Without Mappings:</strong> Use this if your local and remote directory structures are identical</li> <li><strong>With Mappings:</strong> Use this when your local workspace is in a different location than the remote one</li> </ol> <p><strong>Path Mappings Explained:</strong></p> <ul> <li><code class="language-plaintext highlighter-rouge">localRoot</code>: Your local project directory (<code class="language-plaintext highlighter-rouge">${workspaceFolder}</code> refers to your VS Code workspace)</li> <li><code class="language-plaintext highlighter-rouge">remoteRoot</code>: The absolute path to the project on the remote cluster</li> </ul> <p><strong>Important:</strong> Update <code class="language-plaintext highlighter-rouge">remoteRoot</code> to match your actual remote directory path!</p> <h3 id="step-2-set-up-conda-environment-in-vs-code">Step 2: Set Up Conda Environment in VS Code</h3> <p>To ensure VS Code uses the correct Python interpreter:</p> <p><strong>If Conda isn’t recognized by VS Code:</strong></p> <p>Initialize Conda in your shells (run in <strong>Anaconda Prompt</strong> on Windows):</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>conda init powershell
conda init cmd.exe
</code></pre></div></div> <p><strong>Select the Python Interpreter:</strong></p> <ol> <li>Restart VS Code</li> <li>Open Command Palette: <code class="language-plaintext highlighter-rouge">Ctrl+Shift+P</code> (Windows/Linux) or <code class="language-plaintext highlighter-rouge">Cmd+Shift+P</code> (Mac)</li> <li>Type and select: <strong>“Python: Select Interpreter”</strong></li> <li>Choose your conda environment: <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>~\miniconda3\envs\vulnewdata\python.exe  (Windows)
~/miniconda3/envs/vulnewdata/bin/python  (Linux/Mac)
</code></pre></div> </div> </li> </ol> <p>Now you can activate your conda environment directly in VS Code’s integrated terminal!</p> <h3 id="step-3-sync-local-and-remote-code">Step 3: Sync Local and Remote Code</h3> <p><strong>Critical:</strong> Your local code must match the remote code, or breakpoints won’t align correctly!</p> <p>Before debugging, always sync your repository:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Pull latest changes from remote repository</span>
git pull origin main

<span class="c"># Or if you have local changes, commit and push them first</span>
git add <span class="nb">.</span>
git commit <span class="nt">-m</span> <span class="s2">"Your changes"</span>
git push origin main

<span class="c"># Then pull on the cluster</span>
ssh cluster_node <span class="s2">"cd /path/to/project &amp;&amp; git pull"</span>
</code></pre></div></div> <p><strong>Best Practice:</strong> Use Git as the single source of truth. Both your local machine and cluster should pull from the same repository.</p> <h3 id="step-4-set-up-ssh-port-forwarding">Step 4: Set Up SSH Port Forwarding</h3> <p>This is the crucial step that connects your local VS Code to the remote DebugPy server.</p> <p><strong>Create the SSH Tunnel:</strong></p> <p>Open a terminal on your local machine and run:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ssh <span class="nt">-L</span> 55678:localhost:5678 cluster_node
</code></pre></div></div> <p><strong>Understanding the Command:</strong></p> <ul> <li><code class="language-plaintext highlighter-rouge">-L 55678:localhost:5678</code> - Forward local port <code class="language-plaintext highlighter-rouge">55678</code> to remote port <code class="language-plaintext highlighter-rouge">5678</code></li> <li><code class="language-plaintext highlighter-rouge">cluster_node</code> - Your SSH alias or <code class="language-plaintext highlighter-rouge">user@hostname</code></li> </ul> <p>This creates a secure tunnel: VS Code connects to <code class="language-plaintext highlighter-rouge">localhost:55678</code>, which forwards to the cluster’s <code class="language-plaintext highlighter-rouge">5678</code> where DebugPy is listening.</p> <p><strong>Verify the Connection:</strong></p> <p>Use the modern <code class="language-plaintext highlighter-rouge">ss</code> command (socket statistics):</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ss <span class="nt">-tln</span> | <span class="nb">grep </span>55678
</code></pre></div></div> <p>You should see:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>LISTEN    0    128    127.0.0.1:55678    0.0.0.0:*
</code></pre></div></div> <p><strong>Alternative with netstat (older systems):</strong></p> <p>If <code class="language-plaintext highlighter-rouge">ss</code> is not available, you can use the traditional <code class="language-plaintext highlighter-rouge">netstat</code> command:</p> <p>Windows PowerShell:</p> <div class="language-powershell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">netstat</span><span class="w"> </span><span class="nt">-a</span><span class="w"> </span><span class="nt">-n</span><span class="w"> </span><span class="o">|</span><span class="w"> </span><span class="n">select-string</span><span class="w"> </span><span class="s2">"55678"</span><span class="w">
</span></code></pre></div></div> <p>Linux/Mac:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>netstat <span class="nt">-a</span> <span class="nt">-n</span> | <span class="nb">grep </span>55678
</code></pre></div></div> <p>Expected output:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>TCP    127.0.0.1:55678        0.0.0.0:0              LISTENING
</code></pre></div></div> <p><strong>Keep This Terminal Open!</strong> If you close it, the tunnel closes and debugging stops.</p> <hr/> <h2 id="part-3-start-debugging">Part 3: Start Debugging!</h2> <p>Now that everything is set up, let’s start debugging.</p> <h3 id="the-debugging-workflow">The Debugging Workflow</h3> <p><strong>On the Remote Cluster:</strong></p> <ol> <li>Ensure your screen session is running (if you detached)</li> <li>Start your Python script with DebugPy: <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>python <span class="nt">-m</span> debugpy <span class="nt">--wait-for-client</span> <span class="nt">--listen</span> 0.0.0.0:5678 your_script.py
</code></pre></div> </div> </li> <li>You should see output indicating DebugPy is waiting: <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Waiting for debugger attach...
</code></pre></div> </div> </li> </ol> <p><strong>On Your Local Machine:</strong></p> <ol> <li>Ensure SSH port forwarding is active (the tunnel terminal is open)</li> <li>Open your project in VS Code</li> <li>Set breakpoints by clicking in the left margin next to line numbers</li> <li>Go to the <strong>Run and Debug</strong> tab (or press <code class="language-plaintext highlighter-rouge">Ctrl+Shift+D</code>)</li> <li>Select <strong>“Python Debugger: Remote Attach”</strong> from the dropdown</li> <li>Click the green play button ▶ or press <code class="language-plaintext highlighter-rouge">F5</code></li> </ol> <p><strong>Success!</strong> VS Code should connect, and you’ll see:</p> <ul> <li>Your breakpoint indicators turn red (active)</li> <li>The debug toolbar appears</li> <li>Variables panel populates</li> <li>Your code pauses at breakpoints</li> </ul> <h3 id="using-the-debugger">Using the Debugger</h3> <p>Once connected, you can:</p> <p><strong>Navigate Through Code:</strong></p> <ul> <li><strong>Continue</strong> (<code class="language-plaintext highlighter-rouge">F5</code>): Resume execution until next breakpoint</li> <li><strong>Step Over</strong> (<code class="language-plaintext highlighter-rouge">F10</code>): Execute current line, don’t enter functions</li> <li><strong>Step Into</strong> (<code class="language-plaintext highlighter-rouge">F11</code>): Enter function calls to debug them</li> <li><strong>Step Out</strong> (<code class="language-plaintext highlighter-rouge">Shift+F11</code>): Exit current function</li> </ul> <p><strong>Inspect Variables:</strong></p> <ul> <li>View local and global variables in the Variables panel</li> <li>Hover over variables in the code to see their values</li> <li>Add variables to the Watch panel for continuous monitoring</li> </ul> <p><strong>Evaluate Expressions:</strong></p> <ul> <li>Use the Debug Console to evaluate Python expressions in real-time</li> <li>Type any valid Python code to test hypotheses</li> </ul> <p><strong>Call Stack:</strong></p> <ul> <li>See the execution path that led to the current point</li> <li>Click on different frames to inspect variables at each level</li> </ul> <hr/> <h2 id="example-debugging-a-training-script">Example: Debugging a Training Script</h2> <p>Let’s walk through a practical example of debugging a machine learning training script.</p> <h3 id="remote-cluster-in-screen-session">Remote Cluster (in screen session):</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Navigate to your project</span>
<span class="nb">cd</span> ~/vulnewdata

<span class="c"># Activate environment</span>
conda activate vulnewdata

<span class="c"># Start debugging the training script</span>
python <span class="nt">-m</span> debugpy <span class="nt">--wait-for-client</span> <span class="nt">--listen</span> 0.0.0.0:5678 <span class="se">\</span>
    uncertainty/scripts/train_model.py <span class="nt">--epochs</span> 10 <span class="nt">--dataset</span> data/train.csv
</code></pre></div></div> <h3 id="local-machine">Local Machine:</h3> <ol> <li><strong>Open project in VS Code</strong></li> <li><strong>Set breakpoints</strong> in <code class="language-plaintext highlighter-rouge">train_model.py</code>: <ul> <li>Line where model is initialized</li> <li>Beginning of training loop</li> <li>Loss calculation</li> <li>Validation step</li> </ul> </li> <li> <p><strong>Start SSH tunnel</strong> (separate terminal):</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ssh <span class="nt">-L</span> 55678:localhost:5678 cluster_node
</code></pre></div> </div> </li> <li><strong>Attach debugger</strong> in VS Code: <ul> <li>Press <code class="language-plaintext highlighter-rouge">F5</code> or click the debug play button</li> <li>Select “Python Debugger: Remote Attach”</li> </ul> </li> <li><strong>Debug your code:</strong> <ul> <li>Inspect model parameters</li> <li>Check tensor shapes</li> <li>Verify loss calculations</li> <li>Step through the training loop</li> </ul> </li> </ol> <h3 id="common-debugging-scenarios">Common Debugging Scenarios</h3> <p><strong>Scenario 1: Unexpected NaN Loss</strong></p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Set breakpoint here
</span><span class="n">loss</span> <span class="o">=</span> <span class="nf">criterion</span><span class="p">(</span><span class="n">outputs</span><span class="p">,</span> <span class="n">labels</span><span class="p">)</span>
</code></pre></div></div> <p>In Debug Console, inspect:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">&gt;&gt;&gt;</span> <span class="n">outputs</span>
<span class="nf">tensor</span><span class="p">([...])</span>  <span class="c1"># Check for NaN or inf values
</span><span class="o">&gt;&gt;&gt;</span> <span class="n">labels</span><span class="p">.</span><span class="n">shape</span>
<span class="n">torch</span><span class="p">.</span><span class="nc">Size</span><span class="p">([</span><span class="mi">32</span><span class="p">,</span> <span class="mi">10</span><span class="p">])</span>  <span class="c1"># Verify dimensions
</span><span class="o">&gt;&gt;&gt;</span> <span class="p">(</span><span class="n">outputs</span> <span class="o">!=</span> <span class="n">outputs</span><span class="p">).</span><span class="nf">any</span><span class="p">()</span>  <span class="c1"># Check for NaN
</span></code></pre></div></div> <p><strong>Scenario 2: Dimension Mismatch</strong></p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Set breakpoint before the error
</span><span class="n">x</span> <span class="o">=</span> <span class="nf">model</span><span class="p">(</span><span class="n">inputs</span><span class="p">)</span>
<span class="c1"># Inspect x.shape in Variables panel
</span><span class="n">y</span> <span class="o">=</span> <span class="nf">classifier</span><span class="p">(</span><span class="n">x</span><span class="p">)</span>  <span class="c1"># Dimension mismatch here
</span></code></pre></div></div> <p>Check in Variables panel or Debug Console:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">&gt;&gt;&gt;</span> <span class="n">x</span><span class="p">.</span><span class="n">shape</span>
<span class="n">torch</span><span class="p">.</span><span class="nc">Size</span><span class="p">([</span><span class="mi">32</span><span class="p">,</span> <span class="mi">512</span><span class="p">])</span>
<span class="o">&gt;&gt;&gt;</span> <span class="n">classifier</span><span class="p">.</span><span class="n">in_features</span>
<span class="mi">256</span>  <span class="c1"># Ah, there's the problem!
</span></code></pre></div></div> <hr/> <h2 id="troubleshooting-common-issues">Troubleshooting Common Issues</h2> <h3 id="issue-1-vs-code-cant-connect-to-debug-server">Issue 1: VS Code Can’t Connect to Debug Server</h3> <p><strong>Symptoms:</strong></p> <ul> <li>“Connection refused” or timeout error</li> <li>Debugger doesn’t attach</li> </ul> <p><strong>Solutions:</strong></p> <ol> <li> <p><strong>Verify DebugPy is running on cluster:</strong></p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>netstat <span class="nt">-a</span> <span class="nt">-n</span> | <span class="nb">grep </span>5678
</code></pre></div> </div> </li> <li> <p><strong>Check SSH tunnel is active:</strong></p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Local machine</span>
netstat <span class="nt">-a</span> <span class="nt">-n</span> | <span class="nb">grep </span>55678
</code></pre></div> </div> </li> <li><strong>Ensure ports match:</strong> <ul> <li>Remote: <code class="language-plaintext highlighter-rouge">5678</code> (or your chosen port)</li> <li>Local tunnel: <code class="language-plaintext highlighter-rouge">ssh -L 55678:localhost:5678</code></li> <li>VS Code <code class="language-plaintext highlighter-rouge">launch.json</code>: <code class="language-plaintext highlighter-rouge">"port": 55678</code></li> </ul> </li> <li><strong>Firewall issues:</strong> Ensure cluster firewall allows port 5678</li> </ol> <h3 id="issue-2-breakpoints-are-gray-not-active">Issue 2: Breakpoints Are Gray (Not Active)</h3> <p><strong>Symptoms:</strong></p> <ul> <li>Breakpoints appear gray/hollow</li> <li>Code doesn’t pause at breakpoints</li> </ul> <p><strong>Solutions:</strong></p> <ol> <li> <p><strong>Check path mappings in <code class="language-plaintext highlighter-rouge">launch.json</code>:</strong></p> <div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nl">"pathMappings"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
    </span><span class="p">{</span><span class="w">
        </span><span class="nl">"localRoot"</span><span class="p">:</span><span class="w"> </span><span class="s2">"${workspaceFolder}"</span><span class="p">,</span><span class="w">
        </span><span class="nl">"remoteRoot"</span><span class="p">:</span><span class="w"> </span><span class="s2">"/home/username/vulnewdata"</span><span class="w">  </span><span class="err">//</span><span class="w"> </span><span class="err">Must</span><span class="w"> </span><span class="err">match</span><span class="w"> </span><span class="err">exactly!</span><span class="w">
    </span><span class="p">}</span><span class="w">
</span><span class="p">]</span><span class="w">
</span></code></pre></div> </div> </li> <li> <p><strong>Verify files are in sync:</strong></p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git status  <span class="c"># On both local and remote</span>
</code></pre></div> </div> </li> <li> <p><strong>Use absolute paths:</strong> Try specifying exact paths instead of <code class="language-plaintext highlighter-rouge">${workspaceFolder}</code></p> </li> </ol> <h3 id="issue-3-ssh-connection-drops">Issue 3: SSH Connection Drops</h3> <p><strong>Symptoms:</strong></p> <ul> <li>Debugging suddenly stops</li> <li>“Connection lost” message</li> </ul> <p><strong>Solutions:</strong></p> <ol> <li> <p><strong>Use screen on the cluster:</strong></p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>screen <span class="nt">-S</span> debug_session
python <span class="nt">-m</span> debugpy <span class="nt">--wait-for-client</span> <span class="nt">--listen</span> 0.0.0.0:5678 script.py
<span class="c"># Detach with Ctrl+A, D</span>
</code></pre></div> </div> </li> <li> <p><strong>Configure SSH keepalive</strong> in <code class="language-plaintext highlighter-rouge">~/.ssh/config</code>:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Host cluster_node
    ServerAliveInterval 60
    ServerAliveCountMax 10
</code></pre></div> </div> </li> <li> <p><strong>Use persistent SSH tunnel:</strong></p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ssh <span class="nt">-N</span> <span class="nt">-L</span> 55678:localhost:5678 cluster_node
</code></pre></div> </div> <p>(<code class="language-plaintext highlighter-rouge">-N</code> means no remote command, just tunneling)</p> </li> </ol> <h3 id="issue-4-port-already-in-use">Issue 4: Port Already in Use</h3> <p><strong>Symptoms:</strong></p> <ul> <li>“Address already in use” error</li> </ul> <p><strong>Solutions:</strong></p> <ol> <li> <p><strong>Find and kill the process:</strong></p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># On cluster</span>
lsof <span class="nt">-ti</span>:5678 | xargs <span class="nb">kill</span> <span class="nt">-9</span>

<span class="c"># On local machine</span>
lsof <span class="nt">-ti</span>:55678 | xargs <span class="nb">kill</span> <span class="nt">-9</span>  <span class="c"># Mac/Linux</span>
netstat <span class="nt">-ano</span> | findstr :55678   <span class="c"># Windows (then use taskkill)</span>
</code></pre></div> </div> </li> <li> <p><strong>Use different ports:</strong></p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># On cluster</span>
python <span class="nt">-m</span> debugpy <span class="nt">--wait-for-client</span> <span class="nt">--listen</span> 0.0.0.0:5679 script.py

<span class="c"># On local machine</span>
ssh <span class="nt">-L</span> 55679:localhost:5679 cluster_node

<span class="c"># Update launch.json port to 55679</span>
</code></pre></div> </div> </li> </ol> <h3 id="issue-5-wrong-python-interpreter">Issue 5: Wrong Python Interpreter</h3> <p><strong>Symptoms:</strong></p> <ul> <li>Import errors for packages you know are installed</li> <li>Different Python version than expected</li> </ul> <p><strong>Solutions:</strong></p> <ol> <li> <p><strong>Verify remote Python:</strong></p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>which python
python <span class="nt">--version</span>
</code></pre></div> </div> </li> <li> <p><strong>Explicitly use conda python:</strong></p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>conda activate vulnewdata
which python  <span class="c"># Should show conda env path</span>
python <span class="nt">-m</span> debugpy ...
</code></pre></div> </div> </li> <li> <p><strong>Update VS Code interpreter:</strong></p> <ul> <li><code class="language-plaintext highlighter-rouge">Ctrl+Shift+P</code> → “Python: Select Interpreter”</li> <li>Choose the correct conda environment</li> </ul> </li> </ol> <hr/> <h2 id="best-practices">Best Practices</h2> <h3 id="dos-">Do’s ✅</h3> <ol> <li><strong>Always use screen sessions</strong> for remote debugging</li> <li><strong>Keep code synchronized</strong> between local and remote via Git</li> <li><strong>Use descriptive debug configurations</strong> in <code class="language-plaintext highlighter-rouge">launch.json</code></li> <li><strong>Set conditional breakpoints</strong> for specific scenarios</li> <li><strong>Use logpoints</strong> instead of print statements (they don’t require code changes)</li> <li><strong>Document your debug port assignments</strong> if working in a shared cluster</li> <li><strong>Close debug sessions properly</strong> to free up resources</li> </ol> <h3 id="donts-">Don’ts ❌</h3> <ol> <li><strong>Don’t hardcode paths</strong> - use variables like <code class="language-plaintext highlighter-rouge">${workspaceFolder}</code></li> <li><strong>Don’t debug in production environments</strong> - use development/staging clusters</li> <li><strong>Don’t leave debug servers running</strong> - they consume resources</li> <li><strong>Don’t commit <code class="language-plaintext highlighter-rouge">launch.json</code> with personal paths</strong> - use relative paths or .gitignore</li> <li><strong>Don’t debug with print statements</strong> when you can use VS Code’s tools</li> <li><strong>Don’t forget to pull latest code</strong> before debugging</li> <li><strong>Don’t close the SSH tunnel terminal</strong> while debugging</li> </ol> <hr/> <h2 id="advanced-tips">Advanced Tips</h2> <h3 id="tip-1-debug-with-command-line-arguments">Tip 1: Debug with Command-Line Arguments</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>python <span class="nt">-m</span> debugpy <span class="nt">--wait-for-client</span> <span class="nt">--listen</span> 0.0.0.0:5678 script.py <span class="se">\</span>
    <span class="nt">--epochs</span> 10 <span class="se">\</span>
    <span class="nt">--batch-size</span> 32 <span class="se">\</span>
    <span class="nt">--learning-rate</span> 0.001
</code></pre></div></div> <h3 id="tip-2-conditional-breakpoints">Tip 2: Conditional Breakpoints</h3> <p>Right-click on a breakpoint in VS Code and set a condition:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">epoch</span> <span class="o">&gt;</span> <span class="mi">5</span>  <span class="c1"># Only break after epoch 5
</span><span class="n">loss</span> <span class="o">&gt;</span> <span class="mf">1.0</span>  <span class="c1"># Only break if loss is high
</span><span class="n">batch_idx</span> <span class="o">==</span> <span class="mi">100</span>  <span class="c1"># Break at specific batch
</span></code></pre></div></div> <h3 id="tip-3-logpoints">Tip 3: Logpoints</h3> <p>Instead of adding print statements, right-click in the margin and choose “Add Logpoint”:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">Loss</span><span class="p">:</span> <span class="p">{</span><span class="n">loss</span><span class="p">.</span><span class="nf">item</span><span class="p">()},</span> <span class="n">Accuracy</span><span class="p">:</span> <span class="p">{</span><span class="n">acc</span><span class="p">}</span>
</code></pre></div></div> <p>This prints to the Debug Console without modifying code!</p> <h3 id="tip-4-multiple-debug-sessions">Tip 4: Multiple Debug Sessions</h3> <p>Debug multiple processes simultaneously:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Terminal 1: Debug main training</span>
ssh <span class="nt">-L</span> 55678:localhost:5678 cluster_node

<span class="c"># Terminal 2: Debug data preprocessing</span>
ssh <span class="nt">-L</span> 55679:localhost:5679 cluster_node
</code></pre></div></div> <p>Add separate configurations in <code class="language-plaintext highlighter-rouge">launch.json</code> for each.</p> <h3 id="tip-5-debug-jupyter-notebooks">Tip 5: Debug Jupyter Notebooks</h3> <p>You can also debug Jupyter notebooks running on the cluster:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># On cluster</span>
python <span class="nt">-m</span> debugpy <span class="nt">--listen</span> 0.0.0.0:5678 <span class="nt">-m</span> jupyter notebook <span class="nt">--no-browser</span> <span class="nt">--port</span><span class="o">=</span>8888
</code></pre></div></div> <p>Then connect with both Jupyter port forwarding and debug port forwarding!</p> <hr/> <h2 id="quick-reference">Quick Reference</h2> <h3 id="essential-commands">Essential Commands</h3> <p><strong>Remote Cluster:</strong></p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Start debug server</span>
python <span class="nt">-m</span> debugpy <span class="nt">--wait-for-client</span> <span class="nt">--listen</span> 0.0.0.0:5678 script.py

<span class="c"># Check if listening</span>
netstat <span class="nt">-a</span> <span class="nt">-n</span> | <span class="nb">grep </span>5678

<span class="c"># Kill debug process</span>
pkill <span class="nt">-f</span> debugpy
</code></pre></div></div> <p><strong>Local Machine:</strong></p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># SSH tunnel</span>
ssh <span class="nt">-L</span> 55678:localhost:5678 cluster_node

<span class="c"># Check tunnel</span>
netstat <span class="nt">-a</span> <span class="nt">-n</span> | <span class="nb">grep </span>55678

<span class="c"># Persistent tunnel</span>
ssh <span class="nt">-N</span> <span class="nt">-L</span> 55678:localhost:5678 cluster_node
</code></pre></div></div> <p><strong>VS Code:</strong></p> <ul> <li>Open Debug panel: <code class="language-plaintext highlighter-rouge">Ctrl+Shift+D</code></li> <li>Start debugging: <code class="language-plaintext highlighter-rouge">F5</code></li> <li>Toggle breakpoint: <code class="language-plaintext highlighter-rouge">F9</code></li> <li>Step over: <code class="language-plaintext highlighter-rouge">F10</code></li> <li>Step into: <code class="language-plaintext highlighter-rouge">F11</code></li> <li>Step out: <code class="language-plaintext highlighter-rouge">Shift+F11</code></li> <li>Continue: <code class="language-plaintext highlighter-rouge">F5</code></li> </ul> <hr/> <h2 id="additional-resources">Additional Resources</h2> <ul> <li><a href="https://code.visualstudio.com/docs/python/debugging">VS Code Python Debugging</a></li> <li><a href="https://github.com/microsoft/debugpy">DebugPy Documentation</a></li> <li><a href="https://www.ssh.com/academy/ssh/tunneling-example">SSH Port Forwarding Guide</a></li> </ul> <hr/> <h2 id="getting-help">Getting Help</h2> <p>If you encounter issues:</p> <ol> <li>Check this guide’s troubleshooting section</li> <li>Verify all prerequisites are met</li> <li>Test with a simple Python script first</li> <li>Ask in our lab Slack/Discord channel</li> <li>File an issue in the lab’s GitHub repository</li> </ol> <p>Happy debugging! 🐛✨</p> <hr/> <p><strong>Maintained by:</strong> CUNY MASS Lab<br/> <strong>Last Updated:</strong> October 7, 2025<br/> <strong>Questions?</strong> Contact the lab administrators or the authors of the blog post.</p>]]></content><author><name>Josemar Ochoa and Vitaliy Tei</name></author><category term="tutorials"/><category term="debugging"/><category term="vscode"/><category term="python"/><category term="remote-development"/><category term="cluster"/><category term="tutorial"/><summary type="html"><![CDATA[A comprehensive guide to debugging Python code remotely on computing clusters using VS Code, DebugPy, and SSH port forwarding]]></summary></entry><entry><title type="html">Setting Up Python Development Environment for Lab Research</title><link href="https://cunymasslab.github.io/blog/2025/python-environment-setup/" rel="alternate" type="text/html" title="Setting Up Python Development Environment for Lab Research"/><published>2025-09-26T12:00:00+00:00</published><updated>2025-09-26T12:00:00+00:00</updated><id>https://cunymasslab.github.io/blog/2025/python-environment-setup</id><content type="html" xml:base="https://cunymasslab.github.io/blog/2025/python-environment-setup/"><![CDATA[<p>Welcome to the CUNY MASS Lab! This tutorial will guide you through setting up a robust Python development environment for your research work. We’ll use <strong>Conda</strong> to manage system-level environments and explore two popular tools for managing Python dependencies: <strong>pip</strong> and <strong>uv</strong>.</p> <h2 id="why-environment-management-matters">Why Environment Management Matters</h2> <p>When working on research projects, especially in machine learning and data science, you’ll often need:</p> <ul> <li>Different Python versions for different projects</li> <li>Isolated package dependencies to avoid conflicts</li> <li>Reproducible environments that your collaborators can recreate</li> <li>Clean separation between projects</li> <li>Distinction between system-level and Python package isolation</li> </ul> <p><strong>System vs. Package Environments:</strong> It’s important to understand two layers of isolation. <strong>System environments</strong> manage the Python interpreter itself along with system-level libraries and dependencies (like CUDA drivers, compilers, and native binaries). <strong>Python package environments</strong> manage pure Python packages and their dependencies. Conda excels at creating system environments—for example, it can install CUDA libraries, different Python versions, and compiled packages like NumPy with optimized BLAS libraries. In contrast, pip and uv focus on Python package management within an existing Python installation. Conda actually provides both layers, while pip/uv only handle the package layer.</p> <p>This is where environment management tools become essential.</p> <hr/> <h2 id="prerequisites">Prerequisites</h2> <p>Before we begin, ensure you have:</p> <ul> <li><strong>Windows 10/11</strong> (for local development) or <strong>Linux</strong> (for cluster work)</li> <li><strong>Git</strong> installed on your system</li> <li>Access to your lab’s computing cluster (if applicable)</li> </ul> <hr/> <h2 id="part-1-setting-up-conda">Part 1: Setting Up Conda</h2> <p><a href="https://docs.conda.io/en/latest/">Conda</a> is an open-source package and environment management system that works across platforms. We’ll use <strong>Miniconda</strong>, a minimal installer for Conda.</p> <h3 id="installing-miniconda">Installing Miniconda</h3> <h4 id="on-windows">On Windows</h4> <ol> <li>Download Miniconda from the <a href="https://www.anaconda.com/docs/getting-started/miniconda/install">official website</a></li> <li>Run the installer</li> <li><strong>Important:</strong> Check “Add Miniconda to PATH” during installation (or use Anaconda Prompt)</li> <li>Open <strong>Anaconda Prompt</strong> (or Command Prompt if added to PATH)</li> <li>Verify installation:</li> </ol> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>conda <span class="nt">--version</span>
</code></pre></div></div> <h4 id="on-linux-cluster">On Linux (Cluster)</h4> <p>SSH into your cluster and run:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Download Miniconda installer</span>
wget https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-x86_64.sh

<span class="c"># Run the installer</span>
bash Miniconda3-latest-Linux-x86_64.sh

<span class="c"># Follow the prompts, accept the license, and confirm the installation location</span>
<span class="c"># When asked "Do you wish to update your shell profile to automatically initialize conda?" answer yes</span>

<span class="c"># Reload your shell configuration</span>
<span class="nb">source</span> ~/.bashrc

<span class="c"># Verify installation</span>
conda <span class="nt">--version</span>
</code></pre></div></div> <h3 id="accepting-conda-terms-of-service-if-required">Accepting Conda Terms of Service (if required)</h3> <p>Recent versions of Conda may require accepting Terms of Service:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>conda tos accept <span class="nt">--override-channels</span> <span class="nt">--channel</span> https://repo.anaconda.com/pkgs/main
conda tos accept <span class="nt">--override-channels</span> <span class="nt">--channel</span> https://repo.anaconda.com/pkgs/r
conda tos accept <span class="nt">--override-channels</span> <span class="nt">--channel</span> https://repo.anaconda.com/pkgs/msys2
</code></pre></div></div> <h3 id="creating-your-first-environment">Creating Your First Environment</h3> <p>Let’s create an environment called <code class="language-plaintext highlighter-rouge">research-env</code> with Python 3.10:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Create environment with specific Python version</span>
conda create <span class="nt">-n</span> research-env <span class="nv">python</span><span class="o">=</span>3.10 <span class="nt">-y</span>

<span class="c"># Activate the environment</span>
conda activate research-env

<span class="c"># Your prompt should now show (research-env)</span>
</code></pre></div></div> <h3 id="installing-core-scientific-packages-with-conda">Installing Core Scientific Packages with Conda</h3> <p>Conda is excellent for installing complex scientific packages with C/C++ dependencies:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>conda <span class="nb">install</span> <span class="nt">-y</span> numpy pandas scikit-learn matplotlib jupyter
</code></pre></div></div> <h3 id="verifying-your-conda-environment">Verifying Your Conda Environment</h3> <p>Test that everything works:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>python <span class="nt">-c</span> <span class="s2">"import numpy, pandas, sklearn, matplotlib; print('Environment setup successful ✅')"</span>
</code></pre></div></div> <h3 id="useful-conda-commands">Useful Conda Commands</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># List all environments</span>
conda <span class="nb">env </span>list

<span class="c"># Deactivate current environment</span>
conda deactivate

<span class="c"># Remove an environment</span>
conda <span class="nb">env </span>remove <span class="nt">-n</span> research-env

<span class="c"># Update conda itself</span>
conda update conda

<span class="c"># List packages in current environment</span>
conda list
</code></pre></div></div> <h3 id="exporting-and-sharing-environments">Exporting and Sharing Environments</h3> <p>To make your environment reproducible:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Export environment to a file</span>
conda <span class="nb">env export</span> <span class="o">&gt;</span> environment.yml

<span class="c"># Share environment.yml with collaborators</span>
<span class="c"># They can recreate it with:</span>
<span class="c"># conda env create -f environment.yml</span>
</code></pre></div></div> <p><strong>Example <code class="language-plaintext highlighter-rouge">environment.yml</code>:</strong></p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">name</span><span class="pi">:</span> <span class="s">research-env</span>
<span class="na">channels</span><span class="pi">:</span>
  <span class="pi">-</span> <span class="s">defaults</span>
<span class="na">dependencies</span><span class="pi">:</span>
  <span class="pi">-</span> <span class="s">python=3.10</span>
  <span class="pi">-</span> <span class="s">numpy</span>
  <span class="pi">-</span> <span class="s">pandas</span>
  <span class="pi">-</span> <span class="s">scikit-learn</span>
  <span class="pi">-</span> <span class="s">matplotlib</span>
  <span class="pi">-</span> <span class="s">jupyter</span>
  <span class="pi">-</span> <span class="s">pip</span>
  <span class="pi">-</span> <span class="na">pip</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">some-pip-only-package</span>
</code></pre></div></div> <hr/> <h2 id="part-2-managing-dependencies-with-pip">Part 2: Managing Dependencies with pip</h2> <p><a href="https://pip.pypa.io/en/stable/">pip</a> is the standard package installer for Python. It’s bundled with Python and comes pre-installed in Conda environments.</p> <h3 id="why-use-pip-with-conda">Why Use pip with Conda?</h3> <p>While Conda can install Python packages, pip is often necessary because:</p> <ul> <li>Many packages are only available on PyPI (Python Package Index)</li> <li>Some packages release newer versions on PyPI first</li> <li>Your project may have dependencies only available via pip</li> </ul> <h3 id="using-pip-in-a-conda-environment">Using pip in a Conda Environment</h3> <p><strong>Best Practice:</strong> Always activate your Conda environment before using pip!</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Activate your environment</span>
conda activate research-env

<span class="c"># Verify pip is from your conda environment</span>
which pip  <span class="c"># Linux/Mac</span>
where pip  <span class="c"># Windows</span>

<span class="c"># Expected output should show path inside your conda environment</span>
<span class="c"># e.g., ~/miniconda3/envs/research-env/bin/pip</span>
</code></pre></div></div> <h3 id="installing-packages-with-pip">Installing Packages with pip</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Install a single package</span>
pip <span class="nb">install </span>requests

<span class="c"># Install a specific version</span>
pip <span class="nb">install </span><span class="nv">torch</span><span class="o">==</span>2.0.0

<span class="c"># Install from requirements file</span>
pip <span class="nb">install</span> <span class="nt">-r</span> requirements.txt

<span class="c"># Install in editable mode (for development)</span>
pip <span class="nb">install</span> <span class="nt">-e</span> <span class="nb">.</span>
</code></pre></div></div> <h3 id="creating-a-requirementstxt-file">Creating a requirements.txt File</h3> <p>A <code class="language-plaintext highlighter-rouge">requirements.txt</code> file lists all pip-installed packages:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Generate requirements.txt from current environment</span>
pip freeze <span class="o">&gt;</span> requirements.txt

<span class="c"># Install all packages from requirements.txt</span>
pip <span class="nb">install</span> <span class="nt">-r</span> requirements.txt
</code></pre></div></div> <p><strong>Example <code class="language-plaintext highlighter-rouge">requirements.txt</code>:</strong></p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>numpy==1.24.3
pandas==2.0.2
scikit-learn==1.3.0
matplotlib==3.7.1
torch==2.0.0
transformers==4.30.0
</code></pre></div></div> <h3 id="managing-dependencies">Managing Dependencies</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># List installed packages</span>
pip list

<span class="c"># Show details about a package</span>
pip show numpy

<span class="c"># Upgrade a package</span>
pip <span class="nb">install</span> <span class="nt">--upgrade</span> numpy

<span class="c"># Uninstall a package</span>
pip uninstall numpy
</code></pre></div></div> <h3 id="common-pip-issues-and-solutions">Common pip Issues and Solutions</h3> <h4 id="issue-pip-installs-to-wrong-location">Issue: pip installs to wrong location</h4> <p><strong>Solution:</strong> Make sure your conda environment is activated first!</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>conda activate research-env
which pip  <span class="c"># Verify correct location</span>
pip <span class="nb">install </span>package-name
</code></pre></div></div> <h4 id="issue-permission-denied-errors">Issue: Permission denied errors</h4> <p><strong>Solution:</strong> Never use <code class="language-plaintext highlighter-rouge">sudo pip</code>! Use conda environments or virtual environments instead.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Wrong: sudo pip install package-name</span>
<span class="c"># Right: activate environment first</span>
conda activate research-env
pip <span class="nb">install </span>package-name
</code></pre></div></div> <h4 id="issue-conflicting-dependencies">Issue: Conflicting dependencies</h4> <p><strong>Solution:</strong> Create a fresh environment:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>conda create <span class="nt">-n</span> fresh-env <span class="nv">python</span><span class="o">=</span>3.10
conda activate fresh-env
pip <span class="nb">install</span> <span class="nt">-r</span> requirements.txt
</code></pre></div></div> <hr/> <h2 id="part-3-managing-dependencies-with-uv">Part 3: Managing Dependencies with uv</h2> <p><a href="https://github.com/astral-sh/uv">uv</a> is a modern, extremely fast Python package installer and resolver, written in Rust. It’s designed to be a drop-in replacement for pip but with significant performance improvements.</p> <h3 id="why-consider-uv">Why Consider uv?</h3> <ul> <li><strong>Speed:</strong> Faster than pip</li> <li><strong>Reliable:</strong> Better dependency resolution</li> <li><strong>Compatible:</strong> Works as a drop-in replacement for pip</li> <li><strong>Modern:</strong> Built with Rust for performance and reliability</li> </ul> <h3 id="installing-uv">Installing uv</h3> <h4 id="on-windows-1">On Windows</h4> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Using pip in your conda environment</span>
conda activate research-env
pip <span class="nb">install </span>uv

<span class="c"># Or using the standalone installer</span>
powershell <span class="nt">-c</span> <span class="s2">"irm https://astral.sh/uv/install.ps1 | iex"</span>
</code></pre></div></div> <h4 id="on-linux">On Linux</h4> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Using pip in your conda environment</span>
conda activate research-env
pip <span class="nb">install </span>uv

<span class="c"># Or using curl</span>
curl <span class="nt">-LsSf</span> https://astral.sh/uv/install.sh | sh
</code></pre></div></div> <p>Verify installation:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>uv <span class="nt">--version</span>
</code></pre></div></div> <h3 id="using-uv-in-your-workflow">Using uv in Your Workflow</h3> <p>uv commands mirror pip commands, making it easy to switch:</p> <h4 id="installing-packages">Installing Packages</h4> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Activate your conda environment first!</span>
conda activate research-env

<span class="c"># Install a package (replaces: pip install numpy)</span>
uv pip <span class="nb">install </span>numpy

<span class="c"># Install multiple packages</span>
uv pip <span class="nb">install </span>numpy pandas scikit-learn

<span class="c"># Install from requirements.txt</span>
uv pip <span class="nb">install</span> <span class="nt">-r</span> requirements.txt

<span class="c"># Install specific version</span>
uv pip <span class="nb">install</span> <span class="s2">"torch==2.0.0"</span>
</code></pre></div></div> <h4 id="creating-requirements-files">Creating Requirements Files</h4> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Generate requirements (replaces: pip freeze)</span>
uv pip freeze <span class="o">&gt;</span> requirements.txt

<span class="c"># Show package information</span>
uv pip show numpy

<span class="c"># List installed packages</span>
uv pip list
</code></pre></div></div> <h4 id="upgrading-and-uninstalling">Upgrading and Uninstalling</h4> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Upgrade a package</span>
uv pip <span class="nb">install</span> <span class="nt">--upgrade</span> numpy

<span class="c"># Uninstall a package</span>
uv pip uninstall numpy
</code></pre></div></div> <h3 id="uv-vs-pip-when-to-use-each">uv vs pip: When to Use Each?</h3> <table> <thead> <tr> <th>Use Case</th> <th>Recommendation</th> </tr> </thead> <tbody> <tr> <td>Small projects, quick installs</td> <td>Either pip or uv</td> </tr> <tr> <td>Large projects with many dependencies</td> <td>uv (faster)</td> </tr> <tr> <td>CI/CD pipelines</td> <td>uv (much faster)</td> </tr> <tr> <td>Installing compiled packages (numpy, scipy)</td> <td>pip or conda (more tested)</td> </tr> <tr> <td>Maximum compatibility</td> <td>pip</td> </tr> <tr> <td>Maximum speed</td> <td>uv</td> </tr> </tbody> </table> <h3 id="example-workflow-with-uv">Example Workflow with uv</h3> <p>Let’s set up a deep learning project using Conda + uv:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 1. Create conda environment with system dependencies</span>
conda create <span class="nt">-n</span> ml-project <span class="nv">python</span><span class="o">=</span>3.10 <span class="nt">-y</span>
conda activate ml-project

<span class="c"># 2. Install heavy scientific packages with conda</span>
conda <span class="nb">install</span> <span class="nt">-y</span> numpy scipy matplotlib jupyter

<span class="c"># 3. Install uv</span>
pip <span class="nb">install </span>uv

<span class="c"># 4. Use uv for pure Python packages</span>
uv pip <span class="nb">install </span>transformers datasets wandb
uv pip <span class="nb">install </span>torch torchvision

<span class="c"># 5. Generate requirements</span>
uv pip freeze <span class="o">&gt;</span> requirements.txt
</code></pre></div></div> <h3 id="creating-a-hybrid-environmentyml">Creating a Hybrid environment.yml</h3> <p>Combine the best of both worlds:</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">name</span><span class="pi">:</span> <span class="s">ml-project</span>
<span class="na">channels</span><span class="pi">:</span>
  <span class="pi">-</span> <span class="s">defaults</span>
  <span class="pi">-</span> <span class="s">conda-forge</span>
<span class="na">dependencies</span><span class="pi">:</span>
  <span class="pi">-</span> <span class="s">python=3.10</span>
  <span class="pi">-</span> <span class="s">numpy</span>
  <span class="pi">-</span> <span class="s">scipy</span>
  <span class="pi">-</span> <span class="s">matplotlib</span>
  <span class="pi">-</span> <span class="s">jupyter</span>
  <span class="pi">-</span> <span class="s">pip</span>
  <span class="pi">-</span> <span class="na">pip</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">uv</span> <span class="c1"># Install uv via pip</span>
<span class="c1"># Then use uv for remaining packages:</span>
<span class="c1"># uv pip install -r requirements.txt</span>
</code></pre></div></div> <hr/> <h2 id="practical-example-setting-up-for-lab-projects">Practical Example: Setting Up for Lab Projects</h2> <p>Let’s walk through setting up an environment for a typical research project in our lab.</p> <h3 id="scenario-machine-learning-for-vulnerability-detection">Scenario: Machine Learning for Vulnerability Detection</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 1. Create conda environment</span>
conda create <span class="nt">-n</span> vulndetect <span class="nv">python</span><span class="o">=</span>3.10 <span class="nt">-y</span>
conda activate vulndetect

<span class="c"># 2. Install scientific computing stack via conda</span>
conda <span class="nb">install</span> <span class="nt">-y</span> numpy pandas scikit-learn matplotlib jupyter

<span class="c"># 3. Install uv for faster package management</span>
pip <span class="nb">install </span>uv

<span class="c"># 4. Clone your research repository</span>
git clone git@github.com:cunymasslab/your-project.git
<span class="nb">cd </span>your-project

<span class="c"># 5. Install project dependencies with uv</span>
uv pip <span class="nb">install</span> <span class="nt">-r</span> requirements.txt

<span class="c"># 6. Install your package in development mode</span>
uv pip <span class="nb">install</span> <span class="nt">-e</span> <span class="nb">.</span>

<span class="c"># 7. Verify installation</span>
python <span class="nt">-c</span> <span class="s2">"import numpy, pandas, sklearn; print('Ready for research! ✅')"</span>

<span class="c"># 8. Start Jupyter</span>
jupyter notebook
</code></pre></div></div> <h3 id="working-on-the-cluster">Working on the Cluster</h3> <p>For long-running experiments on the cluster:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># SSH to cluster</span>
ssh cluster_node

<span class="c"># Set up conda (first time only)</span>
wget https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-x86_64.sh
bash Miniconda3-latest-Linux-x86_64.sh
<span class="nb">source</span> ~/.bashrc

<span class="c"># Create environment</span>
conda create <span class="nt">-n</span> vulndetect <span class="nv">python</span><span class="o">=</span>3.10 <span class="nt">-y</span>
conda activate vulndetect

<span class="c"># Install packages</span>
conda <span class="nb">install</span> <span class="nt">-y</span> numpy pandas scikit-learn matplotlib
pip <span class="nb">install </span>uv
<span class="nb">cd </span>your-project
uv pip <span class="nb">install</span> <span class="nt">-r</span> requirements.txt

<span class="c"># Use screen for long-running jobs</span>
screen <span class="nt">-S</span> experiment1
<span class="c"># Your command here</span>
<span class="c"># Detach with Ctrl+A, Ctrl+D</span>
</code></pre></div></div> <hr/> <h2 id="best-practices-summary">Best Practices Summary</h2> <h3 id="dos-">Do’s ✅</h3> <ol> <li><strong>Always use virtual environments</strong> - Never install packages globally</li> <li><strong>Activate environment before installing</strong> - Avoid pip/uv installing to wrong location</li> <li><strong>Keep requirements files updated</strong> - Document your dependencies</li> <li><strong>Use conda for system libraries</strong> - NumPy, SciPy, CUDA, etc.</li> <li><strong>Use pip/uv for pure Python packages</strong> - Transformers, requests, etc.</li> <li><strong>Pin versions for reproducibility</strong> - Specify exact versions in production</li> <li><strong>Export environments</strong> - Share <code class="language-plaintext highlighter-rouge">environment.yml</code> with collaborators</li> </ol> <h3 id="donts-">Don’ts ❌</h3> <ol> <li><strong>Don’t use <code class="language-plaintext highlighter-rouge">sudo pip install</code></strong> - This breaks system Python</li> <li><strong>Don’t mix conda and pip carelessly</strong> - Install pip packages after conda packages</li> <li><strong>Don’t ignore dependency conflicts</strong> - They’ll cause runtime errors</li> <li><strong>Don’t commit virtual environments</strong> - Use <code class="language-plaintext highlighter-rouge">.gitignore</code> for <code class="language-plaintext highlighter-rouge">venv/</code>, <code class="language-plaintext highlighter-rouge">.conda/</code></li> <li><strong>Don’t skip testing</strong> - Verify imports work after setup</li> </ol> <hr/> <h2 id="troubleshooting-common-issues">Troubleshooting Common Issues</h2> <h3 id="issue-conda-command-not-found">Issue: “conda: command not found”</h3> <p><strong>Solution:</strong></p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Reload shell configuration</span>
<span class="nb">source</span> ~/.bashrc  <span class="c"># Linux</span>
<span class="c"># or restart terminal on Windows</span>
</code></pre></div></div> <h3 id="issue-packages-installing-to-system-python">Issue: Packages installing to system Python</h3> <p><strong>Solution:</strong></p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>conda activate your-env-name
which python  <span class="c"># Verify correct Python</span>
which pip     <span class="c"># Verify correct pip</span>
</code></pre></div></div> <h3 id="issue-dependency-conflicts">Issue: Dependency conflicts</h3> <p><strong>Solution:</strong></p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Create fresh environment</span>
conda create <span class="nt">-n</span> fresh-env <span class="nv">python</span><span class="o">=</span>3.10
conda activate fresh-env
<span class="c"># Install packages one by one, starting with largest frameworks</span>
</code></pre></div></div> <h3 id="issue-slow-pip-installs">Issue: Slow pip installs</h3> <p><strong>Solution:</strong></p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Switch to uv!</span>
pip <span class="nb">install </span>uv
uv pip <span class="nb">install</span> <span class="nt">-r</span> requirements.txt
</code></pre></div></div> <hr/> <h2 id="additional-resources">Additional Resources</h2> <ul> <li><a href="https://docs.conda.io/">Conda Documentation</a></li> <li><a href="https://pip.pypa.io/">pip Documentation</a></li> <li><a href="https://github.com/astral-sh/uv">uv GitHub Repository</a></li> <li><a href="https://packaging.python.org/">Python Packaging Guide</a></li> </ul> <hr/> <h2 id="getting-help">Getting Help</h2> <p>If you encounter issues:</p> <ol> <li>Check this guide’s troubleshooting section</li> <li>Search error messages online (Stack Overflow, GitHub Issues)</li> <li>Ask in our lab Slack/Discord channel</li> <li>Reach out to senior lab members</li> <li>File an issue in the lab’s GitHub repository</li> </ol> <p>Happy coding! 🐍✨</p> <hr/> <p><strong>Maintained by:</strong> CUNY MASS Lab<br/> <strong>Last Updated:</strong> December 8, 2025<br/> <strong>Questions?</strong> Contact the lab administrators or the authors of this post.</p>]]></content><author><name>Vitaliy Tei and Josemar Ochoa</name></author><category term="tutorials"/><category term="python"/><category term="conda"/><category term="pip"/><category term="uv"/><category term="tutorial"/><category term="environment"/><summary type="html"><![CDATA[A comprehensive guide for newcomers to set up Python environments using Conda, pip, and uv for reproducible research]]></summary></entry></feed>